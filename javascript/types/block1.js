'use strict';
// Example of String Interpolation.
let firstName = "Ethan";

// When we embed a string within backticks,
// we can add variables and perform
// string interpolation.
let greeting = `Hello ${firstName}!`;
console.log(greeting);